# Stop at this directory — do not look for parent configs outside the module.
config.stopBubbling = true

# Mark generated members so coverage and static analysis tools skip them.
lombok.addLombokGeneratedAnnotation = true

# @NonNull throws NullPointerException, matching what the JDK would have done.
lombok.nonNull.exceptionType = NullPointerException
