# Pinned deliberately: PaddleOCR's return shape changed between 2.x and 3.x,
# and an unpinned install is the most common way this service silently starts
# returning empty results. app.py handles both shapes, but pin anyway.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

paddlepaddle==2.6.2
paddleocr==2.9.1

Pillow==11.1.0
PyMuPDF==1.25.2          # PDF page rasterisation
